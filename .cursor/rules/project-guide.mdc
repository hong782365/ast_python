---
alwaysApply: false
---
## 项目级规则（ast_python）

### 项目总览
- **目标**: 基于 WebSocket 的语音到语音（S2S）翻译 Python 客户端 Demo。
- **主要入口**: [ast_demo.py](mdc:ast_demo.py)（推荐），辅助示例: [test_demo.py](mdc:test_demo.py)。
- **Proto 定义与生成**: 参见 [protos/HOWTO.md](mdc:protos/HOWTO.md) 与生成脚本 [protos/build_python.sh](mdc:protos/build_python.sh)。
- **生成代码位置**: 例如 [python_protogen/products/understanding/ast/ast_service_pb2.py](mdc:python_protogen/products/understanding/ast/ast_service_pb2.py)。

### 环境与依赖
- **Python**: 3.x
- **依赖安装**:
```bash
pip3 install websockets protobuf grpcio grpcio-tools
```

### 生成 Protobuf/gRPC 代码（可选）
项目已包含生成产物；如需更新 `.proto` 后重新生成：
```bash
bash protos/build_python.sh
```
- 该脚本会输出到 `python_protogen/` 并修复导入前缀；同时确保创建 `__init__.py`。
- 运行时通过在入口文件中动态 `sys.path.append('python_protogen')` 进行导入。

### 运行示例
1) **配置鉴权**：在 [ast_demo.py](mdc:ast_demo.py) 中 `Config` 填写 `ws_url`、`app_key`、`access_key`、`resource_id`。
   - 建议将密钥改为从环境变量读取，避免提交明文。
2) **准备音频**：输入推荐为 16kHz/16-bit/单声道 WAV（示例：[test_audio.wav](mdc:test_audio.wav)）。
3) **启动**：
```bash
python3 ast_demo.py
```
- **输出**：生成到 `output/translate_audio_00001.opus`，同时在日志中打印累计文本。

### 关键实现要点
- **连接**：使用 `websockets.connect`，在 Header 携带 `X-Api-App-Key`、`X-Api-Access-Key`、`X-Api-Resource-Id`、`X-Api-Connect-Id`。
- **协议**：请求/响应由 Protobuf 定义（`TranslateRequest`/`TranslateResponse`）。
- **发送策略**：按约 100ms 分片（3200 bytes）异步发送音频数据，接收端增量返回音频与文本。
- **会话控制**：开始 `StartSession`，多次 `TaskRequest`，结束 `FinishSession`；注意处理失败/取消与 `X-Tt-Logid`。

### 目录约定与修改建议
- 不直接修改 `python_protogen/` 下生成文件；如需变更，请调整 `.proto` 后重新生成。
- 运行时代码已将 `python_protogen/` 动态加入 `sys.path`，确保导入正常。
- 产物默认写入 `output/`，若不存在会自动创建。

### 常见问题排查
- **ImportError**：先执行 `protos/build_python.sh`，并确认入口脚本已将 `python_protogen/` 加入 `sys.path`。
- **认证失败**：校验 `app_key/access_key/resource_id` 与服务端环境；确认 Header 名称正确。
- **无音频输出**：确认输入 WAV 采样率/位深/声道；查看是否持续收到带 `data` 的响应；检查是否收到 `SessionFinished`。

